a = int(input()) #расстояние между шнурками 
b = int(input()) #расстояние между дырочками в ряду
c = int(input()) #длина шнурка для банта
d = int(input()) #кол-во дырочек в ряду
n = ((2*a*d)-a) + 2*c + (2*b)*(d-1)
print(n)
# (2*a*d)-a на каждую дырку в ряду идёт двойной шнурок кроме первого
# 2*c два конца
# 2*b*(d-1) два ряда , поэтому удваеавем
